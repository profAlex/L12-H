1. Оборачиваем в класс роутер - называем его PostsHandler (например)

2. Добавляем соответствующие TYPES в файл определений

3. Создаем раздельные файлы для Query и Command сервисов и репозиториев - не забываем @injectable()

4. Байндим к контейнеру экземпляры хэндлера, сервисов и репозиториев container.bind(TYPES.PostsHandler).to(PostsHandler).inSingletonScope();

5. Ко всем создаваемым и изменяемым файлам которые содержат @injectable и @inject не забываем добавлять import "reflect-metadata"; в самом начале файле перед всем прочими импортами

6.1 Обновить типы и структуры данных
6.2 На основе обновленных структур делаем схему и модель данных для mongoose
